#ifndef UTILS
#define UTILS

/**
 * @author  Simon Lackerbauer
 * @brief   Error message and exiting program with failure
 * @input   Error-message (string)
 * @output  exits program with exit failure
 */
void die(char *message);		

#endif
